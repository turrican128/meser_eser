VITE_MESERESER_API_KEY=

# Set to 1 ONLY on a demo deploy where you want ?mock=<name> to work in
# production builds (it's enabled by default in `npm run dev`). Leave blank
# on real deployments — fixtures are excluded from the bundle when unset.
VITE_ALLOW_MOCK=
